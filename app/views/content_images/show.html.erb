Small (to link to this image, use the tag "!localimage:<%= @image.id %>:small!" in a text edit box)<br/>
<%= image_tag @image.data.url(:small), size: @image.data.image_size(:small) %><br/>
Medium (to link to this image, use the tag "!localimage:<%= @image.id %>:medium!" in a text edit box)<br/>
<%= image_tag @image.data.url(:medium), size: @image.data.image_size(:medium) %><br/>
Large (to link to this image, use the tag "!localimage:<%= @image.id %>:large!" in a text edit box)<br/>
<%= image_tag @image.data.url(:large), size: @image.data.image_size(:large) %><br/>
Description: <%= @image.description %><br/>
<%= link_to "Original Size", @image.data.url %> (to link to this image, use the tag "!localimage:<%= @image.id %>!" in a text edit box)<br/><br/>
Uploaded By: <%= @image.author.username %><br/>
<br/>
<%= link_to "Back", content_images_path %> | <%= link_to "Edit", edit_content_image_path(@image) %>